<upgrade>
    
    
    <!-- fail on Error explicitly set -> this has to be done in order to create a new configured connector -->
    <!-- removes not null constraint from className --> 
    
    <!-- sql for db2 and oracle not yet tested --> 
    
    <sql dataSource="webdesk" failOnError="true" applies="*" excludes="mysql">alter table poconnector alter column className varchar(255) null</sql>
    <sql dataSource="webdesk" failOnError="true" applies="mysql">alter table poconnector modify className varchar(255) null</sql>
</upgrade>

